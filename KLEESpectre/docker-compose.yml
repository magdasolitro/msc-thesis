version: '3'
services:
    kleespectre_mag:
        # image: kleespectre/kleespectre
        container_name: tradingview
        build:
            context: ./dockerfiles
            dockerfile: kleespectre.Dockerfile
        volumes:
            - '../test_files:/home/klee/shareddir/'
        command: '/bin/bash'
        ulimits:
          stack: -1

